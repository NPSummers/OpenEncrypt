[project]
name = "openencrypt"
version = "0.3.0"
description = "Offline encryption prototype in pure Python (Kyber Round3 KEM; experimental)"
readme = "README.md"
requires-python = ">=3.9"
authors = [
	{ name = "IO-Chan" }
]

[project.scripts]
openencrypt = "openencrypt.cli:main"


